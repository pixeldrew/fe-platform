# common bundling works for css
# npm package of components (import statements should to be preserved for webpack to correctly bundle in consuming package) this breaks node server side rendering because of the import statement
# css should be included
# get mocha tests working
@ webpack aem library bundle
@ babel plugin to dynamically import components into mediators
