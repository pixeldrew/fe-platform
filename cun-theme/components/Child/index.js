import React from 'react';
import { Child as OldChild } from '@carnival-abg/platform';

const Child = () => (
    <div>Cunard Child <OldChild message={'whoop there it is'}/> </div>
);

export default Child;
